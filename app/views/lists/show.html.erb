<h1> Mis peliculas para Descargar 🎥🎬📽</H1>
<%= @list.movies.each do | movies | %>
<%= movie.tittle %>
  <%= link_to "delete movies",bookmark_path(movie), method: :delete %>
    <%end%>
  <%= link_to "create movies",new_list_bookmark_path(@list) %>
